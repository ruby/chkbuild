#!/usr/bin/env ruby

require 'build'

Build.def_perm_target("gcc-v") {|b|
  Build.run("gcc", '-v') {|log|
    if /^gcc version (.*)$/ =~ log
      b.update_title(:gcc_version, "(gcc #{$1})")
    end
  }
}

Build.main
