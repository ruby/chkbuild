#!/usr/bin/env ruby

require 'build'

Dep1 = Build.target("lib1", ["dev"], ["stable"]) {|curr_dir|
}

Dep2 = Build.target("lib2", ["dev"], ["stable"]) {|curr_dir|
}

Build.target("app", ["dev"], ["stable"], Dep1, Dep2) {|curr_dir, lib1, lib2|
}
