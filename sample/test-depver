#!/usr/bin/env ruby

require 'build'

Dep1 = Build.perm_target("lib1", ["dev", "stable"]) {|b|
}

Dep2 = Build.perm_target("lib2", ["dev", "stable"]) {|b|
}

Build.perm_target("app", ["dev", "stable"], Dep1, Dep2) {|b, suffix, lib1, lib2|
}
